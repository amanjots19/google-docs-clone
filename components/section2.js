
import DocumentRow from "./DocumentRow"
import { db } from '../firebase'
function Section2({snapshot}) {
    // con[snapshot] = useCollectionOnce(db.collection('userDocs').doc(session.user.email).collection('docs').orderBy('timestamp', 'desc'))
    const fn = () => {
        if(snapshot){
            return 
        })}
    }

    return (

        <div>
            
        </div>
    )
 }

    export default Section2
